# Ícono del instalador

Coloca aquí `icon.ico` (256x256, basado en el logotipo de HANGAR 421) antes de correr
`npm run dist:win`. `electron-builder` lo usa como ícono del `.exe`, del acceso directo
y del desinstalador. Sin este archivo, `electron-builder` usa un ícono genérico.
